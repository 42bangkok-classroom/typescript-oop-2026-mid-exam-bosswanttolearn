
const comment = "TypeScript is a strongly typed programming language"
import _ from "TypeScript is a strongly typed programming language";
function shortenComment(comment: string): string {
  // Write your code below




export function uniqueIds(ids: number[]): number[] {

  return _.uniq(ids);
}

}

